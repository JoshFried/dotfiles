# AeroSpace config — migrated from Amethyst
# Docs: https://nikitabobko.github.io/AeroSpace/guide

# ── General ────────────────────────────────────────────────
start-at-login = true
after-login-command = []
after-startup-command = [
    'exec-and-forget sketchybar --reload',
    'exec-and-forget ~/.config/aerospace-workspace-assign.sh',
]

# Notify SketchyBar on workspace change
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE'
]

# Normalizations
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# Mouse
on-focus-changed = ['move-mouse monitor-lazy-center']
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Layout
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'

# ── Gaps (matches Amethyst padding + margins) ──────────────
[gaps]
inner.horizontal = 20
inner.vertical = 15
outer.left = 20
outer.right = 20
outer.top = [{ monitor.'built-in' = 25 }, 60]
outer.bottom = 20

# ── Key bindings ───────────────────────────────────────────
# mod1 = alt-shift (matches Amethyst mod1)
# mod2 = alt-shift-ctrl (matches Amethyst mod2)

[mode.main.binding]

# Focus windows (mod1 = alt-shift)
alt-shift-h = 'focus left'
alt-shift-j = 'focus down'
alt-shift-k = 'focus up'
alt-shift-l = 'focus right'

# Move windows
alt-shift-ctrl-h = 'move left'
alt-shift-ctrl-j = 'move down'
alt-shift-ctrl-k = 'move up'
alt-shift-ctrl-l = 'move right'

# Resize — enter resize mode with ctrl-shift-r
alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'
ctrl-shift-r = 'mode resize'

# Toggle float (mod1 + t)
alt-shift-t = ['layout floating tiling', 'exec-and-forget ~/.config/aerospace-toast.sh']

# Fullscreen (mod1 + f)
alt-shift-f = 'fullscreen'

# Focus monitor
ctrl-shift-p = 'focus-monitor prev'
ctrl-shift-n = 'focus-monitor next'

# Move window to other monitor (right_option + n = toggle between monitors)
alt-shift-n = 'move-node-to-monitor --wrap-around next'

# Throw + move-to-monitor:
# Throw = alt-shift + N (right_option on laptop/dactyl via Karabiner = alt+shift)
# Move to monitor = hyper + comma/period (via Hammerspoon)
alt-shift-1 = ['move-node-to-workspace 1', 'workspace 1']
alt-shift-2 = ['move-node-to-workspace 2', 'workspace 2']
alt-shift-3 = ['move-node-to-workspace 3', 'workspace 3']
alt-shift-4 = ['move-node-to-workspace 4', 'workspace 4']
alt-shift-5 = ['move-node-to-workspace 5', 'workspace 5']
alt-shift-6 = ['move-node-to-workspace 6', 'workspace 6']
alt-shift-7 = ['move-node-to-workspace 7', 'workspace 7']
alt-shift-8 = ['move-node-to-workspace 8', 'workspace 8']
alt-shift-9 = ['move-node-to-workspace 9', 'workspace 9']
alt-shift-0 = ['move-node-to-workspace 10', 'workspace 10']

# Switch to workspace (ctrl + number)
ctrl-1 = 'workspace 1'
ctrl-2 = 'workspace 2'
ctrl-3 = 'workspace 3'
ctrl-4 = 'workspace 4'
ctrl-5 = 'workspace 5'
ctrl-6 = 'workspace 6'
ctrl-7 = 'workspace 7'
ctrl-8 = 'workspace 8'
ctrl-9 = 'workspace 9'
ctrl-0 = 'workspace 10'

# Balance window sizes
alt-shift-b = 'balance-sizes'

# Join with neighbor (creates nested BSP-like splits)
ctrl-shift-h = 'join-with left'
ctrl-shift-j = 'join-with down'
ctrl-shift-k = 'join-with up'
ctrl-shift-l = 'join-with right'

# Reset layout (flatten tree back to flat tiles)
ctrl-shift-f = ['flatten-workspace-tree', 'exec-and-forget hs -c "hs.alert.show(\"flatten\", 0.8)"']

# Reload config
ctrl-shift-backspace = 'reload-config'

# ── Floating rules (matches Amethyst blacklist) ────────────
[[on-window-detected]]
if.app-id = 'org.hammerspoon.Hammerspoon'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.amazon.ACME'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.systempreferences'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.Music'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.podcasts'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.amazon.Amazon-Chime'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.electron.amazonconnections'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.cisco.anyconnect.gui'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.cisco.anyconnect.macos.acsock'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.raycast.macos'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.Notes'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.MobileSMS'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.github.wez.wezterm'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
run = 'layout tiling'

[[on-window-detected]]
if.app-id = 'com.jetbrains.intellij'
if.window-title-regex-substring = 'Settings'
run = 'layout floating'

# ── Resize mode ────────────────────────────────────────────
[mode.resize.binding]
h = 'resize width -50'
l = 'resize width +50'
j = 'resize height +50'
k = 'resize height -50'
minus = 'resize smart -50'
equal = 'resize smart +50'
esc = 'mode main'
enter = 'mode main'
